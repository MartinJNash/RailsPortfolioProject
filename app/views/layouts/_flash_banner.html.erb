      <div class="row">
        <div class="columns large-12">
          <% flash.each do |key, value| %>
            <div class="alert-box <%= flash_class(key) %>">
              <%= value %>
            </div>
          <% end %>
        </div>
      </div>
